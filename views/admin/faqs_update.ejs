<%- include('header.ejs') %>   
      <main class="dashboard-content">
        <div class="container-fluid px-3 px-lg-4 py-4">
          <div class="page-heading">
            <div class="page-heading-copy">
              <span class="page-icon"><i class="bi bi-file-earmark" aria-hidden="true"></i></span>
              <div>
                <p class="eyebrow mb-1">Starter</p>
                <h1 class="h3 mb-1">FAQ'S Update</h1>
                <p class="text-muted mb-0">A clean starter page for new admin screens.</p>
              </div>
            </div>
            
          </div>
          

          <section class="panel">
            <form action="/admin/faqs_update_save/<%= data.fid %>" method="post">
                <label>Question : </label>
                <input type="text" value="<%= data.question %>" name="question" class="form-control mb-4">
                <label>Answer :</label>
                <input type="text" name="answer" value="<%= data.answer %>" class="form-control mb-4">
                <button class="btn btn-primary">Update</button>
            </form>
          </section>
        </div>
      </main>
<%- include('footer.ejs') %>   